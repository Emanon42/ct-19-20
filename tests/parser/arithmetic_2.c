// struct ab{
//     int kk;
// };

int main(){
4<=b*;
    // int foo;
    // struct ab abb;
    // char* bar;
    // a=*a***b;
    // foo = sizeof(char*) + 10 * 20 / 120 >= 3 - *bar % 93 - (char)a && foo + abb.kk; 
} 
